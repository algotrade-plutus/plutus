# Handoff — 2026-08-26

> **Partly superseded, 2026-08-27.** Two claims below were true when written and are not
> now. **"1314 tests passing"** — the suite is **2,308**; `FEATURES.md`'s header carries the
> current figure and how it was counted. **"All of this rests on QĐ 26, which nobody in this
> repository has read … Phụ lục 2 of QĐ 26 is paywalled and unpublished"** — QĐ 26 and its
> Phụ lục 2 were obtained and read in full later that same day (`FEATURES.md` §19), the
> post-KRX requirement **is** computed (`session/scenario_margin.py`), and as of 2026-08-27
> it has a runtime path (`session/overnight.py`, `ExchangeSession.overnight_margin()`).
> The count of §16.3 gaps below ("**19** of them") is now **27**, four of them added by
> the same repair and named in it. Everything else here still holds; `FEATURES.md` is the
> file to trust on any specific row.


## READ `docs/reference/FEATURES.md` FIRST

**Before you ask the author anything, and before you write a line of code, read
`docs/reference/FEATURES.md`.** It is the implemented-feature inventory: every feature,
where it lives, and a status from a fixed vocabulary that keeps *built and sourced*
separate from *built and assumed*.

Why it exists: a prior session asked the author to supply the derivatives margin-call
thresholds. They were **already implemented and already sourced at high confidence**
(80 / 90 / 100, Article 13 of QĐ 96/QĐ-VSD → QĐ 61 → QĐ 12 → QĐ 26). That question wasted
the author's time and signalled the code had not been read. FEATURES.md is the substitute
for the memory you do not have.

- §1 — "already answered, do not ask these again"
- §3 — **the assumed-value register**: all 71 of them, in one place. Consult it before
  asking for any number
- §16 — deferred work, with the author's decisions recorded verbatim
- §17 — the live defect register
- §19 — **the verification log.** FEATURES.md was adversarially audited on 2026-08-26 by
  four auditors. §19 records what classes of error they found (an invented traceability
  artefact, three wrong counts, overstated confidence, uncited corrections overturning
  graded rows, ~20 missing capabilities) and which sections are still weakest. **Read §19
  before deciding how much weight to put on any single row.**

Then read this file for context, and keep FEATURES.md true in the same commit as any
feature change (its §18).

## What Plutus is

> **A simulated Vietnamese exchange a strategy connects to like a broker API.**
> Submit orders, receive status, and the exchange remembers settlement, margin and the
> dated rulebook so the strategy author does not have to.

Not a backtesting engine — no strategy execution, no portfolio, no P&L. The caller owns
those. Plutus is the counterparty.

## State: Tier 1 and Tier 2 are built and green

**1314 tests passing.** `src/plutus/market/session/` is the simulator. A per-feature
inventory of all ten modules, with status and citations, is `docs/reference/FEATURES.md`:

| Module | What it owns |
|---|---|
| `types.py` | The shared contract. Transition graph, time-in-force table and terminal triggers as **data**, so no module can draw a second graph |
| `rulebook.py` | Per-instant resolution. A venue is `(ticker, ts)`. KRX is a dated edition. Resolution is total: `KNOWN` / `NOT_APPLICABLE` / `UNKNOWN`, never a guess |
| `calendar.py` | VSDC settlement business days. Reproduces the rulebook's own Tết 2026 worked example |
| `ledgers.py` | Encumbrance + tranche holdings + tranche proceeds + the sale advance |
| `orders.py` | The state machine, per-order-type terminal edges |
| `deposit.py` | Segregated deposit; `account_margin_requirement` takes the **whole account** |
| `fills.py` | `FillPolicy` seam: hard, soft, probabilistic, plus an explicit `INDETERMINATE` |
| `charges.py` | Generic dated charge table, per venue |
| `corporate.py` | Dividends, splits, bonus, rights |
| `exchange.py` | `ExchangeSession`: submit / cancel / amend / poll / advance_to |

The headline behaviour works end to end and is pinned: **buy at T0, try to sell the same
day, the exchange refuses because the shares have not settled** — with the mirror test
proving the same sell is accepted once the tranche settles.

## The five locked shapes — and how to verify them

These are the design decisions that cost a rewrite if wrong. Every one was verified the
only way that actually tests it: **one session straddling a regime boundary**, not two
sessions run at two dates.

| Straddle | Result |
|---|---|
| HSX fee 0.0003 → 0.00027 @ 2020-03-19 | 28,650 then 25,785 |
| Round lot 10 → 100 @ 2021-01-04 | `Accepted` then `Rejected(ROUND_LOT)` |
| Settlement → T+2 @ 13:00 @ 2022-08-29 | two parcels in **one** `Holding` |
| VSD IM 0.13 → 0.17 @ 2022-12-15 | PIT 6,500 then 8,500 |

1. **Per-instant resolution.** Forbidden: config-at-load singletons, ticker-keyed venue
   caches. Twelve survivors were catalogued and four fixed; the eight remaining are
   documented as latent with the reason each is harmless.
2. **Encumbrance ledger** — reserve on accept, release on *every* terminal edge.
3. **Tranche-list holdings and proceeds.** Forbidden: scalar pairs.
4. **The order type IS the time-in-force**, per-type terminal edges.
5. **Margin takes the whole account.** `account_margin_requirement(Position(...))` raises
   `TypeError` by design.

## The paper's claim — read this before writing a word of it

Eleven agents surveyed prior art, three tried to refute each claim, and a twelfth
verified the survivors against PDFs and APIs. **Most of what we intended to claim did not
survive.** Full detail in `docs/reference/literature-review.md`; the outline is
`docs/reference/paper-outline.md`.

**Dropped entirely, refuted:**
- *Pluggable fill determination.* NautilusTrader ships `prob_fill_on_limit` in {0, p, 1}
  by default. LEAN, Zipline, RQAlpha, hftbacktest, PyAlgoTrade too.
- *Data-source agnosticism via a published contract.* LEAN's `BaseData`, with
  `SupportedResolutions()` and `IsSparseData()`, is exactly that.
- *Being first to an explicit indeterminate outcome.* Forex Strategy Builder shipped
  `enum BacktestEval {Error, None, Ambiguous, Unknown, Correct}` with a Method Comparator
  **in 2011**.

**Must be hedged, never asserted flat:**
- ~~"first to combine call auctions, price limits and settlement"~~ — **EvoMarket**
  (arXiv:2604.18046, 20 Apr 2026) does exactly that for China A-shares.
- ~~"first matching-engine simulation of a Vietnamese exchange"~~ — Lê Đức Hùng (2013,
  VNU-UET) built a QuickFIX HNX test double.
- The intrabar-ambiguity framing belongs to **Claeys** (SSRN 6240638, verified exact:
  18.47% ambiguous bars, 3,695-point spread).

**The lead claim, which survived attack:**

> Plutus represents a national exchange rulebook as **effective-dated rule editions
> resolved per simulated instant**, so a 2024 backtest and a 2026 backtest execute under
> the rules that actually applied to each — across a real regime change, the KRX cutover
> of 2025-05-05 — and we find no simulator, open-source or commercial, in which any
> **admission** rule is keyed to an effective date.

Its warrant is measured, not asserted: `measurements/dated_rules.py` finds **82.2% of the
HSX equity sample (893,628 of 1,086,518) predates the round-lot change**, so a date-blind
simulator rejects every legal 10-to-90 share order across four fifths of it. Vo & Doan
(2023, PLoS ONE) supply the independent warrant via a difference-in-differences on HOSE's
real 2016 tick-size change.

**Social risk:** the JICCE dashboard paper (Le Hoanh-Su et al., UEL/VNU-HCM) published at
RIVF 2025 and we submit to RIVF'26. Assume they are in the reviewer pool. Cite accurately
and respectfully.

## Results worth not re-deriving

- **Settlement.** `quote.settlementprice` holds the **already-computed** value; the last
  entry on an expiry day *is* the settlement price — read it, do not average it. Measured
  across all 46 post-cutover expiries, the close-as-settlement substitution costs mean
  absolute **0.042%**, worst **0.333%**. VN30F2206, the contract earlier notes leaned on,
  sits near the *worst* of the distribution.
- **Venue mis-attribution.** Extending the tick-grid audit past HSX finds 15,504 off-grid
  UPCoM closes — and **all 15,504 lie exactly on HOSE's grid**. They are HOSE prices with
  a UPCoM label. Two unrelated methods (tick grids here, price bands in the rulebook)
  converge on the same defect and name the same tickers.
- **The two margin paths do NOT disagree** — an earlier revision of this handoff said
  they did, and that was wrong. They are the same functional form, and with the VSD ratio
  frozen at entry a funding multiple in [1.4110, 1.4136] reproduces 29/48/56 exactly. The
  real finding is that **the experiment is malformed**: at funding = 1× the requirement a
  100% call rate is an arithmetic identity, and the legacy series is a drawdown quantile
  at a threshold set by an unsourced 5-point buffer. Neither should be published as a
  call rate. See `docs/reference/margin-model-adjudication.md`.

## Author decisions — 2026-08-26

Recorded verbatim. Full consequences for the code are in `FEATURES.md` §16.1.

> 1. **Equity margin lending is PRIORITY 1.** Not built at all today. Must be built.
> 2. **Securities as margin collateral: CASH ONLY for the MVP.** Note clearly as future work.
> 3. **Government bond futures: ALL bond features are FUTURE WORK.** Very few users of this
>    repo will touch bonds. Note clearly. (The GB contract multiplier was fixed recently --
>    record that it exists but that the bond product as a whole is out of scope for now.)
> 4. **Intraday margin checkpoints: MUST HAVE.** Check the policy very carefully before
>    implementing -- times, what is checked, and whether the KRX cutover changed them.
> 5. **Cure window: default to next session** (our default, stated in the implementation).
>    Making it user-configurable is future work.

Notes a next session needs:

1. **Equity margin lending is greenfield** — verified by grep, no equity-margin symbol
   exists in `src/`. The implementable spec is `docs/reference/equity-margin-spec.md`:
   `imr ≥ 50 %`, `mmr ≥ 30 %`, cure ceiling 3 business days (QĐ 87 Điều 7.1 **alone** —
   TT 120 Điều 9.6 has no day count), term ≤ 3 months, foreigners barred from *ký quỹ*,
   collateral valued at no more than the last close — all VERIFIED against QĐ 87/QĐ-UBCK
   and TT 120/2020, **and every one of them read from a commercial mirror, not from công
   báo**; obtain the gazette copy before any traceability claim in the paper leans on a
   specific clause. **Three things a builder must not miss** (all added after the
   2026-08-26 audit): (i) "⇒ max loan-to-value 50 %" is **DERIVED**, not in the text;
   (ii) **no verified numeric call/force-sell threshold exists at any broker** — the one
   published ladder (DNSE) is a *giao dịch tiền mặt* cash-product table, so `call_level`
   and `force_level` must be unsourced required fields with no defaults; (iii) **TT 120
   Điều 9a** is the foreign-institutional non-prefunded regime — the foreign bar applies
   to *ký quỹ* only, and a simulator that refuses all foreign credit-funded buying is
   wrong. **Do not reuse `deposit.py`**: derivatives clearing margin is a different
   product with a different regulator, custody chain and call test.
2. Cash-only collateral is already how `deposit.py` behaves. Future work is the securities
   leg. ⚠️ **The claim that the VSDC haircuts are "now sourced" (5 % / 30 % / 40 %, in the
   *body* of QĐ 61 Điều 8 and QĐ 26 Điều 9) carries no source, no grade and no retrieval
   note, and it overturns a rulebook row graded UNVERIFIED using a document our own
   rulebook grades `medium (never read)`.** Treat the three percentages as REPORTED until
   QĐ 26 is obtained. `FEATURES.md` §16.1 Decision 2 has the full caveat.
3. GB multipliers (10,000, HIGH) stay; the product does not. GB futures are in any case
   unreachable end-to-end today because every session runs `InvestorClass.INDIVIDUAL`,
   whose GB position cap is 0.
4. **The KRX cutover did change the checkpoints, and our rulebook's value is wrong.**
   Pre-KRX: real-time in-session utilisation against 80/90/100. Post-KRX (QĐ 26 Điều 13.2):
   MR computed after the close, top-up by **09:30 T+1**, checkpoints at **09:30 / 14:00 /
   16:30**, and the utilisation ladder is **abolished for margin** — the test becomes
   binary assets-vs-MR, and 80/90/100 survives only for position limits. The rulebook's
   "09:30 and 14:30" is a broker (Pinetree) error. Seven specific corrections to
   rulebook §6.3 are listed in `FEATURES.md` §16.1. ⚠️ **All of this rests on QĐ 26, which
   nobody in this repository has read** — our own rulebook grades it `medium (never read)`
   — and two of the seven corrections overturn rows graded `high`. `FEATURES.md` §16.1
   Decision 4 now grades each correction individually. **Obtain QĐ 26 before implementing
   from either the current rows or the corrections.** The post-KRX MR **still cannot be
   computed** in any case: Phụ lục 2 of QĐ 26 is paywalled and unpublished.
5. Cure window = next session is implemented and labelled an assumption
   (`broker.py:90`). The 3-day and 5-day figures in the sources are **clearing-member-to-
   VSD deadlines**, not investor cure windows — that CONFLICT is now resolved.
   **It is also already user-configurable**: `BrokerProfile.from_config` accepts
   `margin_cure_window` as a string or an integer session count and raises rather than
   defaulting (`types.py:2449-2484`). Only the broker survey remains outstanding.

## What is left

- **Equity margin lending** (decision 1) — spec written, nothing built.
- **Tier 3 measurements**: the chained band test. ⚠️ The **occupancy census** ("how often
  each rule binds") is now **out of scope** under standing rule 6 — how often a rule fires
  is the user's result to discover with their own strategy, not ours to publish.
- **The paper itself** — explicitly another session's responsibility.
- **Deferred**: the live Calibrator; populating post-KRX rulebook *values* (the mechanism
  exists); foreign-ownership enforcement (tradeoff T1); a broker survey to replace the
  assumed commercial defaults; event-driven callbacks; queue-position matching. Reasons
  for each are in `FEATURES.md` §16.2.
- **Gaps with no decision recorded** — **19** of them, `FEATURES.md` §16.3. These are not
  deferrals; nobody decided them. The loudest: **no covered warrant can be admitted at any
  date** (the band formula is sourced but never derived, so every CW order is
  INDETERMINATE); `load_data_source` cannot construct either shipped adapter; no
  settlement-calendar data ships; monthly/daily charges are never levied; and
  `src/plutus/core/{position,portfolio,transaction,algorithm,bot}.py` **do not import
  outside a pytest run** — `tests/utils.py` shadows the gap, which is standing rule 4
  in its purest form.

## Standing rules for this project

1. **Overclaiming is a defect.** Every unsourced value says so in its own docstring; see
   `BrokerTerms.PROVENANCE` and `margin.PROVENANCE` for the pattern.
2. **Exchange rules and broker terms live in separate objects.** Gazetted-and-dated
   versus commercial-and-per-firm. Never assert a house rule as market law.
3. **Where the rulebook is silent, say so** rather than inventing a rule.
4. **A fix is not done until a test fails without it.** The suite was green through every
   one of the twelve defects found by adversarial review — green tests prove very little
   on their own.
5. **Discarded experiments get recorded.** See the tick-grid dead end in
   `measurements/dated_rules.py`: an invented rule table produced a large, convincing,
   meaningless number.
6. **We do not measure incidence.** An earlier pass computed margin-call incidence
   statistics; it was wrong and has been retracted (`docs/reference/margin-model-
   adjudication.md`). Whether a rule fires often is the **user's** result to discover with
   their own strategy. Our job is to implement policy faithfully. Do not add, propose or
   cite an incidence measurement. Residual measurement claims still living in docstrings
   are logged as defects in `FEATURES.md` §17 (D16, D19).
7. **Keep `docs/reference/FEATURES.md` true in the same commit as any feature change.**
   An inventory that drifts is worse than none, because it will be trusted. The
   maintenance procedure is its §18.
